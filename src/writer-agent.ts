import { articleChatInstruction } from './article-link.js';

export const writerAgentName = 'SupportPages.io';
export const writerAgentType = 'supportpages-io';
/** Claude Code namespaces plugin agents as <plugin>:<agent>. The plugin's MCP
 * configuration sets SUPPORTPAGES_PLUGIN to the plugin name. */
export const pluginName = process.env.SUPPORTPAGES_PLUGIN?.trim() || undefined;
export const writerSubagentType = pluginName ? `${pluginName}:${writerAgentType}` : writerAgentType;

export const writerPermissionInstruction = 'Permission requests take priority over concise reporting and article chat guidance; approval prompts must include the command and access needed. When an operation needs additional permission and the host policy allows requests, use the host native approval mechanism directly with the exact command and a clear reason. A sandbox restriction is not an explicit user denial: request approval rather than immediately reporting failure. Wait for the approval decision and continue only if granted. Do not send a request only to the parent when a native approval prompt is available. If approval is denied, policy forbids requesting it, or this host cannot surface it, stop the blocked action and report permission_blocked to the parent with the failed command, specific cause and recovery needed. Do not retry around a denial, weaken sandbox settings, enable YOLO, or bypass restrictions. Publication consent remains the parent responsibility.';

export const backgroundWriterInstruction = 'Background reporting: prioritize completing the article and resolving blockers over silence. After the entrypoint succeeds, notify the parent of startup with run_id and editor_url using the host messaging facility when available. Send concise milestone updates and report blockers immediately; include technical diagnostics needed for recovery instead of hiding them. Native approval prompts are allowed and must not be suppressed. Return a final result with run_id, status and editor_url. Report success only after the required finish command confirms delivery; otherwise report permission_blocked, delivery_pending, upload_failed or the specific failure. The parent handles the final user-facing response and publication consent.';

export const backgroundParentInstruction = 'For background articles, supervise the delegated writer until it finishes or needs user intervention. Pass the complete task brief directly to the writer, preserve its task identity and completion notification, and record the successful launch with supportpages_update_run. Show the editor link and say launched unless actual startup is confirmed. Keep the main turn active and use the host agent wait/message facilities to receive progress, approval requests and the final result; do not end the turn merely because launch succeeded. Use bounded waits and concise progress updates rather than rapid status polling. Allow the writer to request native approvals directly when host policy permits. Never treat a sandbox restriction as an explicit user denial. If a background writer cannot surface approval, confirm it has stopped, then continue the same article in the foreground using the MCP recovery instructions and normal approval mechanism; foreground execution does not grant extra permissions. If approval is denied or forbidden by policy, report the specific block and stop that action without bypassing it. Inspect the writer final result and saved run: a host Completed notification alone is not success. For a stopped writer that failed or is permission_blocked before generation finished, record event=failed with stopped=true via supportpages_update_run and show the cause and recovery with the editor link. For a successfully generated writer, call supportpages_complete_article and report the confirmed delivery once. Recover delivery_pending or upload_failed through complete_article without regenerating or marking the writer failed. Keep the writer and MCP session alive. If the host cannot supervise a delegated task, use the foreground fallback; do not launch a separate agent CLI. Honor an explicit user request to return immediately, but explain that approvals or completion may require another user turn on hosts that do not wake the parent.';

export function writerReportingInstruction(background: boolean) {
  return (background ? backgroundWriterInstruction : 'Foreground reporting: include the supplied editor_url in your first progress report and your completion or failure report when available; keep the same link on retries. Keep progress concise and follow the article chat guidance.') + ' ' + writerPermissionInstruction;
}

// Shared by the persistent Claude agent and session-scoped --agents definition.
// Model and permissions inherit the session.
export const claudeWriterAgents = {
  [writerAgentType]: {
    description: 'SupportPages Writer agent for illustrated help articles. Use only after supportpages_prepare_article or supportpages_retry_article returns a task brief.',
    model: 'inherit',
    prompt: 'You are the SupportPages.io article writer. Execute the supplied task brief using its workspace, environment, output directory and installed skills. Always run task_brief.entrypoint.shell_command exactly as supplied before reading generation instructions or writing content. The launcher loads the saved run configuration; no environment exports are needed. Run subsequent generation, rendering and validation commands through task_brief.execution so each command gets the correct environment in a fresh shell. If no prepared task brief or entrypoint was supplied, or its check fails, return to the main agent without writing and ask it to call supportpages_prepare_article or supportpages_retry_article. Do not invoke a publicly discovered generation skill or load the private engine directly to bypass preparation. Follow the reporting policy returned by the entrypoint. Provide concise progress and surface blockers promptly in either execution mode. Request native approvals directly when allowed; communication and recovery take priority over silence. Work in the existing application directory without worktree isolation or a new checkout. Produce the complete article and illustrations, then run all required rendering and validation. Do not edit application source, call SupportPages.io MCP tools, upload or publish. The main agent owns the run and handles progress, completion and publication consent. After every required output passes validation, run task_brief.completion.shell_command exactly as supplied and wait for its result before reporting success. This local finish command asks the MCP to perform final validation and draft delivery without giving you credentials. A preview link is not proof of completion. If delivery is pending or fails, return its recovery instructions to the parent; do not claim the article is ready, regenerate, or ask to publish. Only the parent handles publication consent.' + ' ' + writerPermissionInstruction + ' For background execution only: ' + backgroundWriterInstruction + ' ' + articleChatInstruction,
  },
};

export const writerLaunchInstruction = `In Claude Code, select subagent_type="${writerSubagentType}" for the writer, not general-purpose. Set the Agent tool's model parameter to task_brief.claude_model when it is a string; when null, omit the model parameter and inherit the session model. Apply this on initial launches, retries and follow-ups. Do not substitute another model silently if the host cannot use the requested model; explain the limitation. This model setting applies only to Claude Code, not Codex. Begin the task description with "${writerAgentName}" followed by the article topic. Use background execution only when prefer_background is true. If the named agent is unavailable, explain that SupportPages.io setup installs it and Claude may need restarting, then execute the same task brief in the foreground without launching a substitute subagent. If the Agent call returns "Agent type not found", do not repeat that call, prepare a replacement run, or mark the writer started or failed: execute the existing task brief in the foreground and report started only when work actually starts.`;

export const writerAgentFile = `---\nname: ${writerAgentType}\ndescription: ${JSON.stringify(claudeWriterAgents[writerAgentType].description)}\nmodel: inherit\n---\n\n<!-- Managed by SupportPages: article writer v1 -->\n\n${claudeWriterAgents[writerAgentType].prompt}\n`;

export const codexWriterFile = `# Managed by SupportPages: article writer v1\nname = ${JSON.stringify(writerAgentType)}\ndescription = ${JSON.stringify(claudeWriterAgents[writerAgentType].description)}\ndeveloper_instructions = ${JSON.stringify(claudeWriterAgents[writerAgentType].prompt)}\n`;

export const codexWriterLaunchInstruction = `In Codex, use the registered ${writerAgentType} custom agent for this article. Name the delegated task SupportPages.io followed by the article topic. Pass task_brief.codex_model and task_brief.codex_reasoning_effort as the host's model and reasoning_effort spawn overrides when non-null; omit null overrides to use Codex's configured defaults. Start with a fresh context containing the complete task brief so the host can apply explicit model overrides. Pass that brief directly in the subagent tool task/message argument; do not copy it into a temporary JSON file, create a handoff script, or print it through a shell command. The entrypoint already loads the saved run, so launching the writer needs no parent-side file edit. Apply this on retries too. Use background execution only when prefer_background is true. If the host has no custom-agent selector, use a named SupportPages.io task with task_brief.writer_instructions and the same model settings. The installed SupportPages.io coordination skill explicitly requests background delegation when the prepared run prefers it. Do not infer that delegation is disabled merely because the user did not separately request a subagent. If a host tool is missing, a session restriction forbids delegation, or launch fails, explain that specific limitation and run the same task brief in the foreground after stopping any launched writer. Do not silently substitute an unsupported model. Work in the existing directory, without a worktree or a separate agent CLI. The parent handles all MCP calls, previews, draft completion and publication consent; the writer only generates and validates files. Keep the parent's sandbox and permission controls.`;
